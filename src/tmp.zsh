#!/usr/bin/env zsh

typeset -g APP_BASE_PATH=$(pwd); source "${APP_BASE_PATH}"/src/utils/autoload.zsh || exit 1

import @/src/utils/helper.zsh

#!/usr/bin/env zsh

# Generates a unique variab
# Example usage

generate_unique_var_name;

