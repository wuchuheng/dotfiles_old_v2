{
  "default": {
    "Darwin": {
      "x86_64": "${{ prefix }}/bin/macos/v2ray -c ${{ prefix }}/proxy_config.json",
      "arm64": "${{ prefix }}/bin/macos/v2ray -c ${{ prefix }}/proxy_config.json"
    },
    "Linux": {
      "x86_64": "${{ prefix }}/bin/qjs_Linux_x86_64"
    }
  },
  "command_lines": {
    "proxy": "${{ default }}"
  }
}