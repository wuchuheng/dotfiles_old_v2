name: Dotfiles tests

on:
  push:
    branches:
      - dev
      - master

jobs:
  unit_test_in_linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install zsh
        run: sudo apt install -y zsh
      - name: start the unit tests
        run: make unit_test

  installation_in_linux:
    needs: unit_test_in_linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install zsh
        run: sudo apt install -y zsh
      - mame: update subodules
        run: git submodule update
      - name: start the unit tests
        run: make install

  unit_test_in_macos_x86:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install zsh
        run: brew install zsh
      - name: start the unit tests
        run: make unit_test

  installation_in_macos_x86:
    needs: unit_test_in_macos_x86
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install zsh
        run: brew install zsh
      - mame: update subodules
        run: git submodule update
      - name: start the unit tests
        run: make install

#  macos-arm:
#    runs-on: macos-11.0
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v2
#      - name: Run commands on macOS ARM
#        run: |
#          make show_os_type
